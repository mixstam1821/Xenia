services:
  xenia:
    build: .
    ports:
      - "8994:8994"
    volumes:
      - ./backend/data:/app/backend/data
    env_file:
      - ./backend/.env
    user: "${UID:-1000}:${GID:-1000}"
    restart: unless-stopped
 
